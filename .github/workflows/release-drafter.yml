name: 'Release Drafter'

on:
  pull_request:
   branches:
    - release/*
  

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      
      - name: Calculate Tags to Compare
        id: calculate_compare_tags
        run: |
          env
          echo GITHUB_REF $GITHUB_REF
          echo GITHUB_BASE_REF $GITHUB_BASE_REF
          echo GITHUB_HEAD_REF $GITHUB_HEAD_REF
          FROM_TAG_PART1=${GITHUB_BASE_REF#release/*/x}
          TO_TAG=$GITHUB_BASE_REF
          echo From Tag Part 1: $FROM_TAG_PART1
          echo To Tag: $TO_TAG
          echo "::set-output name=TO_TAG::$TO_TAG"
#          echo "::set-output name=TARGET_RELEASE::$TARGET_RELEASE"
      
#      - name: Build Changelog
#        id: github_release
#       uses: mikepenz/release-changelog-builder-action@v1
#        with:
#          failOnError: true
#          configuration: ".github/release_config.json"
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
     
#      - name: Echo Changelog
#        env:
#          CHANGELOG: ${{ steps.github_release.outputs.changelog }}
#        run: echo "$CHANGELOG"          
